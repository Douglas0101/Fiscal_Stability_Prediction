# Dockerfile.mlflow

# 1. Usar a imagem oficial do MLflow como base
FROM ghcr.io/mlflow/mlflow:v2.9.2

# 2. Instalar as dependências necessárias para o nosso ambiente
#    - psycopg2-binary: Para conectar ao banco de dados PostgreSQL.
#    - curl: Para que o comando do healthcheck do Docker Compose funcione.
#    A imagem base é baseada em Debian, então usamos `apt-get`.
RUN apt-get update && apt-get install -y curl && \
    pip install psycopg2-binary && \
    rm -rf /var/lib/apt/lists/*