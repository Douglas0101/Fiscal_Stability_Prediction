# Dockerfile.dashboard (Revisto para React)

# Usamos uma imagem oficial e leve do Nginx como base.
FROM nginx:1.25.3-alpine

# Remove a página de boas-vindas padrão do Nginx.
RUN rm /etc/nginx/conf.d/default.conf

# Copia os ficheiros da nossa aplicação React para o diretório
# que o Nginx serve por defeito.
COPY ./dashboard/index.html /usr/share/nginx/html/index.html

# Expõe a porta 80, que é a porta padrão do Nginx.
EXPOSE 80
